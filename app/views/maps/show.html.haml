= page_title @page_title

%ol.breadcrumb
  %li.breadcrumb-item= link_to 'Maps', maps_path
  %li.breadcrumb-item.active= @map.name

%h1
  = @map.name
  - if can? :update, Map
    = link_to 'Edit', edit_map_path(@map)

= render "games/games_list", games: @map_games, should_paginate: true

