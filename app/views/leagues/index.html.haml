= page_title "Leagues"

%h1 Public Leagues
= render "leagues_list", leagues: @public_leagues

%h1 Private Leagues
= render "leagues_list", leagues: @private_leagues

%br/
- if can? :create, PublicLeague
  = link_to 'New Public League', new_public_league_path
  |
- if can? :create, PrivateLeague
  = link_to 'New Private League', new_private_league_path