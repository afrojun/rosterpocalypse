= page_title "Rosters"

%h1 Your Rosters
- if @my_rosters.any?
  = render 'roster_list', rosters: @my_rosters, show_tournament: true, show_transfers: true
- else
  %p No Rosters found

%br/
- if can? :create, Roster
  = link_to 'New Roster', new_roster_path, class: "btn btn-primary"
