= page_title "Rosters"

%h1 Rosters
- if @my_rosters.any?
  %h2 #{current_user.username}'s Rosters
  = render 'roster_list', rosters: @my_rosters, show_tournament: true
- else
  %p No Rosters found

%br/
- if can? :create, Roster
  = link_to 'New Roster', new_roster_path, class: "btn btn-primary"
