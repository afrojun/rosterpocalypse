- present(@game) do |game|
  %h1 Game Details
  %h3 #{game.map.name} game took #{distance_of_time_in_words(game.duration_s, 0, include_seconds: true)} on #{game.start_date}

  %table.table.table-sm.table-striped
    %thead
      %tr
        %th
        %th Player
        %th Hero
        %th Kills
        %th Deaths
        %th Assists
        %th Time Spent Dead
        - if can?(:update, Game) && can?(:update, PlayerGameDetail)
          %th{:colspan => "2"}
    %tbody
      - game.game_details.each do |details|
        %tr
          %td{class: details.win ? "game-win" : "game-loss"}
          %td= details.player.name
          %td= details.hero.name
          %td= details.solo_kills
          %td= details.deaths
          %td= details.assists
          %td= details.time_spent_dead
          - if can?(:update, Game) && can?(:update, PlayerGameDetail)
            %td= link_to 'Edit', edit_player_game_details_path(details)
            %td= link_to 'Destroy', details, method: :delete, data: { confirm: 'Are you sure?' }