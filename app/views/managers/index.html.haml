= page_title "Managers"

%h1
  Managers
  %small= "(#{Manager.count})"

.center
  = paginate @managers

%table.table.table-sm.table-striped
  %thead
    %tr
      %th Manager
  %tbody
    - @managers.each do |manager|
      %tr
        %td= link_to manager.name, manager
